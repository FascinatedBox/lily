class Point(x: integer, y: integer)
{
    var @x = x
    var @y = y
    define total : integer {
        return @x + @y
    }
}

class Point3D(x: integer, y: integer) < Point(x, y)
{
    var @z = total()
}

var v = Point3D::new(1, 2)
if v.z != 3:
    stderr.write("Failed.\n")
