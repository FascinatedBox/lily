class Point(x: integer, y: integer)
{
    var @x = x
    var @y = y

    define add(add_x: integer, add_y: integer) {
        if 1: {
            @x += add_x
            @y += add_y
        }
    }
}
