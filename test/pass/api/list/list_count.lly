if [1, 2, 3, 4, 5].count{|a| a % 2 == 0} != 2:
    stderr.print("Failed.")

var v = [Some(10), None, Some(20)]

if v.count{|a| a.is_none() } != 1:
    stderr.print("Failed.")

if [].count{|a| true} != 0:
    stderr.print("Failed.")
