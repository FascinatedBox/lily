var v = [3, 2, 1]
var values: list[integer] = []

v.each_index{|i| values.append(i) }

if values != [0, 1, 2]:
    stderr.write("Failed.\n")

if v.each_index{|i| true } != v:
    stderr.write("Failed.\n")
