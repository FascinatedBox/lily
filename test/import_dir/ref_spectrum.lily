define ref_define: Integer { return 10 }

var ref_var = 10

class RefSquarer {
    public define square(a: Integer): Integer { return a + a }
}

scoped enum ScopedOption[A] {
    ScopedSome(A),
    ScopedNone
}

enum FlatOption[A] {
    FlatSome(A),
    FlatNone
}

import "import_dir/var_exporter"
